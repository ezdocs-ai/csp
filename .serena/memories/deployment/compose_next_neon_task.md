User requests: add frontend-next to the single root docker-compose.yml with watch functionality, then provide an in-chat Terraform deployment guide replacing Cloud SQL with NeonDB and list required services. Current docker-compose.yml already contains unvalidated frontend-next dev and frontend-next-prod services from prior work; frontend-next dev uses Dockerfile.dev, port 3000, API_BASE_URL=http://backend:8080, bind mount ./frontend-next:/app and node_modules named volume. Need refine to official Compose develop.watch. Dockerfile.dev uses Bun and starts `bun run dev -- --port 3000 --hostname 0.0.0.0`. Production Dockerfile uses Next standalone, port 8080. Existing Angular service remains port 4200. Terraform currently has Cloud Run/platform/postgresql modules; task interpretation is compose code change plus deployment guide, unless inspection reveals a small required infrastructure change. No cloud commands/resources. No new explanation markdown files.