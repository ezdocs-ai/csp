User explicitly requests removal of all Cloud SQL dependencies. Current failures come from infra/modules/platform/main.tf reading creative-studio-db-password and module postgresql. Removal scope: Terraform postgresql module usage/resources/outputs, cloud-run Cloud SQL volume/env/IAM/vars; backend google.cloud.sql.connector code/dependency/config/tests; bootstrap Cloud SQL proxy and references; docs/config examples. Target DB is manually managed Neon with DATABASE_URL set later. Must preserve local Docker Postgres. Implement DATABASE_URL direct asyncpg/SQLAlchemy support with TLS-compatible URL, likely optional DIRECT_DATABASE_URL for Alembic. No gcloud/cloud changes. Validate tofu offline and backend tests in Docker, coverage >=80, pre-commit.