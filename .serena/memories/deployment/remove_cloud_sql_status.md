Cloud SQL removal completed from active stack. Deleted infra/modules/postgresql. Removed platform DB password data/module and Cloud SQL outputs. Removed cloud-run Cloud SQL volume, env, IAM role, and vars; runtime secrets now receive generic secretAccessor IAM. Backend removed cloud-sql-python-connector dependency/lock packages and all Connector code/config; supports DATABASE_URL for app and DIRECT_DATABASE_URL for Alembic/advisory lock, with local DB_* fallback. Bootstrap proxy/password-secret setup removed. Docs updated. Validation: tofu validate passed; backend full suite 467 passed, 1 skipped, 83.91%; black passed; backend container starts and migrations run against local Compose Postgres. Full pre-commit remains blocked by existing Angular gts spawn eslint ENOENT. Existing csp-genmedia import can now be retried after tofu init -reconfigure; no DB password data source will be evaluated. User intends to set deployed DB URLs manually, so Terraform intentionally does not bind DATABASE_URL secrets.