## Wave 3 (DONE) — all 5 generation studios adopt the frozen primitives

All landed: Image, Video, Upscale, VTO, Audio. Per-feature detail in `parity_impl/wave3_{image,video,upscale,vto,audio}`.

### Lead cleanup after wave 3
- Deleted 4 orphaned pre-primitive components (zero importers verified via grep): `src/components/studio/{generation-form,option-controls,prompt-input,result-panel}.tsx`.
- **Real primitive bug fixed at the source**: `MediaLightbox`'s `variant="comparison"` branch returned early before `ActionsToolbar`, so any `actions` passed to a comparison lightbox were silently dropped (found by the Upscale agent, who worked around it locally). Fixed in `src/components/studio/media-lightbox.tsx`: comparison now renders `ActionsToolbar` too; `ActionsToolbar`'s `variant` prop widened from `Exclude<MediaVariant,"comparison">` to `MediaVariant`. Then removed the Upscale agent's local workaround (duplicate Download/See-more-info buttons + now-dead `DownloadIcon`/`InfoIcon`) in `upscale-studio.tsx` and switched it to the real `actions={{ download, seeMoreInfo }}` prop.
- Investigated and cleared two "unverified" flags from agent logs as false alarms: `/api/upscale`, `/api/upscale/{id}`, `/api/gallery/download` all exist as real Next BFF routes (`app/api/upscale/**`, `app/api/gallery/download/**`) proxying to the backend — agents only checked the raw backend `openapi.json` and missed the Next proxy layer.
- `/terms-of-service` footer link 404s, but this is **pre-existing Angular breakage**, not a Next regression: Angular's own `footer.component.ts` calls `router.navigateByUrl('terms-of-service')` but no such route exists in `app-routing.module.ts` either. Not fixing — Angular doesn't have this page.
- Project-wide `diagnostics`: 0 errors across all touched files (only pre-existing Tailwind v4 shorthand style warnings, consistent with the rest of the codebase).

### Two earlier bugs also fixed this session (before wave 3)
- `src/components/ui/sidebar.tsx` — Tools flyout menu was clipped by unconditional `overflow-hidden` on the collapsed pill; the flyout is `position:absolute` inside it. Fixed: `overflow-hidden` only while collapsed, `overflow-visible` once expanded/hovered.
- Model-selector settings popover in `prompt-input.tsx` (now deleted, logic ported into `FlowPromptBox`) rendered **behind** `OptionToolbar`'s dropdowns (`z-20` vs `z-50`). Fixed by establishing a deliberate stacking convention: `FlowPromptBox` card `z-[55]`, its settings popover `z-[60]`, both above `OptionToolbar`'s `z-50`. All 5 wave-3 agents were briefed on this convention and respected it (verified).

### Known real gaps still open (from agent logs, out of any current write set)
- **VTO**: preset model/garment grids (`/api/source_assets/vto-assets`) will render empty until backend seeds system assets — endpoint is real, not invented. Examples row assets (`upload-photo-*.png`) absent from disk on both apps. MediaLightbox `delete` only clears local state, doesn't call `/api/gallery/bulk-delete`. Garment mutual-exclusion snackbars not ported (no notification infra).
- **Video**: welcome video asset `generate-video-homepage-video.mp4` absent from `public/assets/videos/` (gradient fallback active, never 404s). `/api/gemini/rewrite-prompt` call has no confirmed local BFF handler (best-effort, non-blocking).
- **Audio**: background video asset `abstract-waves.mp4` absent (gradient fallback). "Add your voice" clone flow has no backend endpoint — rendered disabled with tooltip, not fabricated.
- **Image**: `MediaLightbox` doesn't expose active-index to action callbacks, so cross-feature handoff (`onSendToVideo`/`onSendToVto`) always uses output index 0. `assignTags` action omitted (no shared tagger component yet).
- ~~Media-gallery admin route bug~~ **FIXED.** `app/api/admin/media-gallery/route.ts` was calling invented endpoints (`/api/media-items`). Rewrote to call the real backend: `POST /api/gallery/search` (GallerySearchDto, admin search omits `workspace_id`), `POST /api/gallery/bulk-delete`, `POST /api/gallery/items/{id}/restore?item_type=`, `POST /api/admin/cleanup-stuck-jobs`. Required extending the POST body contract with `itemType`/`workspaceId` (both always present on every gallery row) and updating the sole caller `media-gallery-admin.tsx` to pass them.
- ~~Admin dashboard charts~~ **Was a false alarm, now doubly fixed.** `app/api/admin/dashboard/route.ts` DOES already forward `workspace-stats`/`active-users-monthly` (verified by reading it — the earlier memory note was stale). The real bug was in `app/(admin)/admin/page.tsx`: its defensive `WorkspaceStat` type mapped `w.workspace ?? w.name` but the backend's `AdminWorkspaceStats` schema returns `workspaceName` — that key was never checked, so the workspace bar chart always showed "—" labels. Fixed: added `workspaceName` to the type and the fallback chain.

## Session update — build green, logged in, one more bug found+fixed
- `bun run build && bun run lint` **CONFIRMED GREEN** by the user (2 rounds of fixes: a redundant `?? ""` strict-null error in `media-gallery-admin.tsx`, then a `SlotAsset | undefined` strict-null error in `video-studio.tsx`'s `onPickerSelect` — both invisible to `diagnostics`/LSP, only caught by `next build`'s type check. **Confirmed pattern: `diagnostics` cannot be trusted for strict-null issues — `next build` is the only real gate.**).
- Logged into Next (`localhost:3000`) via browser automation as `avei@tridorian.com`. Confirmed `admin` role (`/admin` sidebar link present) and `workflows` role (Tools flyout shows "Workflows" entry).
- **Found + fixed a real bug while verifying login**: `app/api/workspaces/route.ts` only exported `POST`, but `src/lib/workspace/api.ts`'s `listWorkspaces()` (called from both `app/layout.tsx` SSR prefetch and `WorkspaceProvider`'s client-side refresh) does `GET /api/workspaces` → 405 on every page load, silently swallowed by try/catch, leaving the switcher stuck on "Select workspace" forever. Added the missing `GET` handler (mirrors the POST handler's `requireApiClient()` + try/catch pattern). Verified live: switcher now shows "Trd Csp Workspace".
- Also verified live: the sidebar Tools-flyout clipping fix from earlier this session (`overflow-hidden` → conditional) actually works in the browser, not just in code — flyout renders fully outside the collapsed pill.

## Next
1. Phase D: 3 fresh read-only reviewers (visual, interaction/a11y, code/runtime) — build is green, lint is green, login is confirmed with both `admin`+`workflows` roles. No more blockers.
2. Paired-screenshot verification against Angular for the 5 studio pages (Image/Video/Upscale/VTO/Audio) — only ever done for Video before, and Video changed again in wave 3. Do this as part of or alongside Phase D's visual reviewer.
3. If Phase D surfaces regressions, fix only VERIFIED ones — do not preemptively "improve" anything not flagged.
